# Appi runtime-core

Shared background-runtime logic imported by the desktop device agent.

- Error codes
- Execution modes (background-safe / foreground / attention)
- User activity detector
- Login autostart helpers

This package does not replace `apps/device-agent`.

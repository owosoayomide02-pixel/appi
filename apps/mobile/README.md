# Future mobile agent

This directory holds the original Flutter Hello World starter that was in the repository before Appi V0.1.

V0.1 is Windows-first (Next.js dashboard + Python device agent). A mobile-device agent is a later milestone and should speak the same tool protocol and permission model.
